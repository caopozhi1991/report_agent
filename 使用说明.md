# Report Agent 使用说明

## 1. 项目功能

本项目根据账户、成交和持仓 CSV 数据生成投资复盘 PDF，支持：

- 多账户报告生成
- 股票净值曲线和策略核心指标汇总
- 纯股票持仓统计
- ETF 自动剔除
- 股票交易统计（剔除 ETF 和逆回购）
- 持仓结构分析
- 通过 OpenAI 兼容接口调用 DeepSeek 进行交易和持仓分析

生成的报告位于 `reports/` 目录。

## 2. 环境要求

- Windows PowerShell
- Python 3.10 或更高版本
- 已安装项目依赖：`pandas`、`matplotlib`、`python-dotenv`、`openai`

如果尚未安装依赖，可以执行：

```powershell
python -m pip install pandas matplotlib python-dotenv openai
```

建议在项目根目录执行命令。项目根目录是包含 `report_agent/`、`data/`、`cache/` 和 `reports/` 的目录。

## 3. 配置 `.env`

项目根目录已提供 `.env.example` 模板。可以先复制模板，再填写 API Key：

```powershell
Copy-Item .\.env.example .\.env
```

`.env` 配置示例：

```env
OPENAI_API_KEY=替换为新的DeepSeek_API_KEY
OPENAI_BASE_URL=https://api.deepseek.com/v1
OPENAI_MODEL=deepseek-v4-flash
OPENAI_TEMPERATURE=0.3
REPORT_LLM_ENABLED=true
```

配置说明：

| 变量 | 说明 |
| --- | --- |
| `OPENAI_API_KEY` | DeepSeek API Key，必须填写真实值 |
| `OPENAI_BASE_URL` | DeepSeek OpenAI 兼容接口地址 |
| `OPENAI_MODEL` | 使用的模型名称，例如 `deepseek-v4-flash` |
| `OPENAI_TEMPERATURE` | 模型输出随机性，报告建议使用 `0.2` 至 `0.5` |
| `REPORT_LLM_ENABLED` | 是否启用大模型，支持 `true`、`false`、`1`、`0` |

`.env` 已加入 `.gitignore`，不要将 API Key 提交到 Git。API Key 如果曾经出现在聊天记录、截图或日志中，应在服务商控制台撤销并重新生成。

## 4. 输入数据

默认数据目录为 `data/`。程序按账户编号读取以下文件：

```text
{account_id}_account.csv
{account_id}_deals.csv
{account_id}_positions.csv
```

例如账户 `1206016764`：

```text
data/1206016764_account.csv
data/1206016764_deals.csv
data/1206016764_positions.csv
```

程序也兼容带中间批次标识的文件名，例如：

```text
data/1206016764_2_account.csv
data/1206016764_2_deals.csv
data/1206016764_2_positions.csv
```

### 4.1 账户文件字段

支持中文字段或对应英文别名，常用中文字段如下：

```text
总资产, 可用资金, 股票市值, 盈亏
```

### 4.2 成交文件字段

```text
证券代码, 操作, 成交价格, 成交数量, 成交金额
```

`操作` 支持 `买入`、`卖出`、`BUY`、`SELL`、`B`、`S`。

交易统计会剔除逆回购，包括：

- 操作名称包含 `回购` 或 `逆回购` 的记录
- 证券代码以 `204` 开头的记录
- 证券代码以 `1318` 开头的记录

因此，类似 `204001 通用回购卖出` 的大额成交不会计入股票买入、卖出、净投入或交易分析。

### 4.3 持仓文件字段

```text
证券代码, 当前拥股, 成本价, 市值, 盈亏
```

也支持常见英文别名，例如 `code`、`quantity`、`avg_cost`、`market_value`、`profit_loss`。

文件可以使用 UTF-8 或 GB18030 编码。

## 5. 生成单个账户报告

在 PowerShell 中执行：

```powershell
$env:PYTHONIOENCODING = "utf-8"
python -c "from report_agent.agent import MultiAccountReportAgent; print(MultiAccountReportAgent().generate_for_account('1206016764', date='20260819'))"
```

成功后会生成：

```text
reports/1206016764_20260819_report.pdf
```

报告日期建议使用 `YYYYMMDD` 格式，例如：

```text
20260819
```

如果不传 `date`，程序默认使用当天日期：

```powershell
python -c "from report_agent.agent import MultiAccountReportAgent; print(MultiAccountReportAgent().generate_for_account('1206016764'))"
```

## 6. 生成全部账户报告

配置中的账户会由 `generate_all()` 统一生成：

```powershell
$env:PYTHONIOENCODING = "utf-8"
python -c "from report_agent.agent import MultiAccountReportAgent; print(MultiAccountReportAgent().generate_all(date='20260819'))"
```

当前默认账户及初始本金配置在 [report_agent/config.py](report_agent/config.py) 中：

| 账户 | 初始本金 |
| --- | ---: |
| `1219020189` | 600,000 元 |
| `1206016764` | 100,000 元 |

如需新增账户，需要同时在 `initial_capital`、`account_types` 和数据目录中配置对应账户。

### 配置账户昵称

账户昵称在 [report_agent/config.py](report_agent/config.py) 的 `account_names` 中配置：

```python
"account_names": {
  "1219020189": "chenwei",
  "1206016764": "hongta_xiaofeng",
},
```

昵称用于 PDF 页面标题和文件名，账户 ID 仍只用于读取数据、缓存和内部状态。生成的文件名格式为：

```text
股票实盘复盘报告_{昵称}_{YYYYMMDD}.pdf
```

例如：

```text
股票实盘复盘报告_chenwei_20260819.pdf
```

也可以直接运行项目根目录的 `main.py`，它会按脚本内的日期生成全部账户报告：

```powershell
$env:PYTHONIOENCODING = "utf-8"
python .\main.py
```

## 7. 报告内容

生成的 PDF 包含以下主要内容：

1. 净值曲线与策略核心指标汇总
2. 当日交易分析
3. 交易逻辑说明
4. 大模型交易分析
5. 股票持仓明细
6. 持仓结构摘要
7. 大模型持仓分析

所有股票报告均按纯股票口径统计。ETF 会根据证券代码前缀自动剔除，包含常见的 `15`、`16`、`50`、`51`、`52`、`56`、`58` 开头代码。账户 CSV 中的原始 `总资产` 不直接用于纯股票净值；净值使用初始本金加剔除 ETF 后的股票浮盈亏计算。

交易统计还会剔除逆回购，包括操作名称含 `回购` 或 `逆回购`，以及 `204`、`1318` 开头的证券代码。

## 8. 大模型调用说明

启用 `.env` 中的：

```env
REPORT_LLM_ENABLED=true
```

并配置有效的 `OPENAI_API_KEY` 后，报告会分别调用模型进行：

- 当日交易分析
- 当前股票持仓分析

DeepSeek 使用 OpenAI 兼容客户端，因此通过以下配置连接：

```env
OPENAI_BASE_URL=https://api.deepseek.com/v1
```

如果没有配置 API Key，程序会使用离线降级文本，仍然可以生成 PDF，但不会执行真实模型推理。

## 9. 种子数据和缓存

- `seeds/`：用于首次恢复账户状态
- `cache/`：保存各账户各日期的状态和净值历史，缓存文件名统一为 `YYYYMMDD.json`
- `reports/`：保存生成的 PDF

当目标日期没有缓存时，程序会优先读取历史缓存，找不到时再读取对应账户的种子文件。

如果历史缓存中存在错误数据，可能导致净值曲线异常。清理或修正缓存前请先备份 `cache/` 目录。

交易分析会读取账户状态中保存的成交历史，因此报告中的买卖笔数和金额可能包含缓存中的多个历史日期；逆回购会在统计阶段再次被过滤。

## 10. 验证代码

编译主要模块：

```powershell
python -m py_compile `
  .\report_agent\config.py `
  .\report_agent\agent.py `
  .\report_agent\core\data_loader.py `
  .\report_agent\core\pdf_generator.py `
  .\report_agent\analysis\llm\llm_analysis.py
```

运行基础报告验证脚本：

```powershell
$env:PYTHONIOENCODING = "utf-8"
python .\validate_report_agent.py
```

运行种子恢复验证：

```powershell
$env:PYTHONIOENCODING = "utf-8"
python .\verify_seed_resume.py
```

注意：这些验证脚本会写入或覆盖 `data/`、`cache/`、`seeds/` 和 `reports/` 中的测试数据。运行前请备份真实数据。

## 11. 常见问题

### PDF 中没有真实大模型分析

检查以下内容：

1. `.env` 是否位于项目根目录。
2. `REPORT_LLM_ENABLED` 是否为 `true`。
3. `OPENAI_API_KEY` 是否有效。
4. `OPENAI_BASE_URL` 是否为 `https://api.deepseek.com/v1`。
5. 模型名称是否与账号权限匹配。

### 报告持仓数量或市值与预期不一致

优先检查：

1. 日期对应的 CSV 是否为最新快照。
2. 文件名中的账户编号是否正确。
3. `cache/账户编号/` 中是否存在旧缓存。
4. ETF 是否被剔除后再计算纯股票统计。

### 中文显示乱码

在 PowerShell 中先设置：

```powershell
$env:PYTHONIOENCODING = "utf-8"
```

同时确保输入 CSV 使用 UTF-8 或 GB18030 编码。

## 12. 版本说明

### v1.0.1（当前版本）

- 增加 A4 统一页面和动态 PDF 排版，长表格自动分页并减少空白区域。
- 净值按纯股票口径计算，完全剔除 ETF。
- 交易统计剔除 ETF 和逆回购，避免大额逆回购影响买卖金额。
- 支持通过 `.env` 配置 DeepSeek OpenAI 兼容接口。
- 交易分析和持仓分析支持分别调用大模型。
- 统一缓存文件日期格式为 `YYYYMMDD.json`，并兼容读取旧日期格式。
- 增加全路径 `__pycache__` Git 忽略规则。
